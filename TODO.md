# TODO

## Configuración manual pendiente (una sola vez)

- [x] Ejecutar `supabase/migrations/0001_schema.sql` y `0002_rls.sql` en el SQL Editor de Supabase (en ese orden).
- [x] Supabase Dashboard → Authentication → Providers → Email → desactivar "Allow new users to sign up".
- [x] Supabase Dashboard → Authentication → Users → crear las 2 cuentas (`amda.97@gmail.com`, `lauraplaza403@gmail.com`).
- [x] Ejecutar los `UPDATE` de renombrado de perfiles indicados al final de `0001_schema.sql` (Alvaro / Lauri).
- [x] GitHub → Settings → Pages → Build and deployment → Source = "GitHub Actions".
- [x] GitHub → Settings → Secrets and variables → Actions → añadir `VITE_SUPABASE_URL` y `VITE_SUPABASE_ANON_KEY`.
- [ ] Copiar `.env.example` a `.env.local` y rellenarlo para desarrollo local (`npm run dev`).
- [x] Ejecutar `supabase/migrations/0003_subcategoria_facturas.sql` en el SQL Editor de Supabase (añade "Facturas" a Vivienda).
- [x] Ejecutar `supabase/migrations/0004_reemplazar_recurrentes_por_categorias_fijas.sql` en el SQL Editor de Supabase (elimina la funcionalidad de recurrentes y define "gastos fijos" por subcategoría — sección 6).
- [x] Ejecutar `supabase/migrations/0005_aportaciones_desde_ahorro.sql` en el SQL Editor de Supabase (nuevas columnas `es_ahorro`/`es_traspaso` en subcategorías, y redefine `aportaciones_objetivo` con el trigger que mantiene `acumulado` sincronizado — sección 7).
- [x] Ejecutar `supabase/migrations/0006_es_inversion.sql` en el SQL Editor de Supabase (nueva columna `es_inversion` en subcategorías, para la métrica "Inversión total del mes" del Dashboard — sección 9).
- [x] Ejecutar `supabase/migrations/0007_ingreso_condicional.sql` en el SQL Editor de Supabase (nueva columna `es_ingreso_condicional` en subcategorías; redefine "Ingreso real" — sección 5).
- [x] Ejecutar `supabase/migrations/0008_periodo_pago.sql` en el SQL Editor de Supabase (nueva columna `es_primer_dia_mes` en movimientos; redefine "mes" como periodo personal de nómina a nómina — sección 3).
- [x] Ejecutar `supabase/migrations/0009_patrimonio.sql` en el SQL Editor de Supabase (nuevas tablas `posiciones_patrimonio`/`patrimonio_historico`, RLS y función `generar_snapshot_patrimonio` — nueva página "Patrimonio", sección 15).
- [x] Ejecutar `supabase/migrations/0010_patrimonio_tae.sql` en el SQL Editor de Supabase (columna `tae` en `posiciones_patrimonio`, `precio_actual_unitario` pasa a opcional; cálculo automático del valor actual por rentabilidad conocida para Fondo Monetario/Cuenta Remunerada/Cuenta de Ahorro — sección 15).
- [x] ~~Registrarse en Twelve Data~~ (ya no se usa, se cambió a Yahoo Finance — ver más abajo).
- [x] Desplegar la Edge Function de precios automáticos (`npx supabase login`, `npx supabase link --project-ref <ref>`, `npx supabase functions deploy actualizar-precios-patrimonio`, luego `npx supabase secrets set TWELVE_DATA_API_KEY=...` con la clave del paso anterior — ver cabecera de `supabase/functions/actualizar-precios-patrimonio/index.ts`).
- [x] Ejecutar `supabase/migrations/0011_patrimonio_cron_precios.sql` en el SQL Editor de Supabase (guardando la `service_role` key en Vault con `vault.create_secret`) — sustituido por `0012_patrimonio_cron_horario.sql`, ver siguiente punto.
- [x] Redesplegar la Edge Function (`npx supabase functions deploy actualizar-precios-patrimonio`, ya registra la hora del cron).
- [x] Ejecutar `supabase/migrations/0012_patrimonio_cron_horario.sql` en el SQL Editor de Supabase (desprograma los 2 jobs de la 0011 y programa uno nuevo cada hora, UTC 6:00-22:00 — cubre siempre las 8:00-23:00 de España sin depender de `cron.timezone`, que dio error `55P02` al intentar fijarlo: solo se puede cambiar al arrancar el servidor, no vía SQL Editor). El secreto de Vault tenía guardado por error el JWT clásico de `service_role` en vez de la clave nueva `sb_secret_...` que la función recibe inyectada — corregido con `vault.update_secret`.
- [x] Ejecutar `supabase/migrations/0013_patrimonio_error_precio.sql` en el SQL Editor de Supabase (columna `error_precio` en `posiciones_patrimonio` — la Edge Function ya redesplegada la usa para guardar el motivo cuando no consigue actualizar un precio, en vez de fallar en silencio).
- [x] Ejecutar `supabase/migrations/0014_patrimonio_moneda.sql` en el SQL Editor de Supabase (columna `moneda` en `posiciones_patrimonio`, EUR por defecto — la Edge Function ya redesplegada la usa para convertir de USD a EUR con el tipo de cambio EUR/USD cuando el ticker da el precio en dólares).
- [x] Ejecutar `supabase/migrations/0015_patrimonio_fondo_indexado_rv_rf.sql` en el SQL Editor de Supabase (divide el tipo "Fondo Indexado" en "Fondo Indexado RV" y "Fondo Indexado RF"; migra las posiciones existentes separando por nombre) — verificado, resultado correcto.
- [x] Ejecutar `supabase/migrations/0016_patrimonio_ventas.sql` en el SQL Editor de Supabase (nueva tabla `ventas_patrimonio` y los RPC `registrar_venta_patrimonio`/`crear_posicion_financiada_patrimonio` — vender un activo con reparto FIFO y financiar una compra desde una cuenta existente, sección 15).
- [x] Ejecutar `supabase/migrations/0017_patrimonio_historico_precios.sql` en el SQL Editor de Supabase (tabla `precios_historico_activo`, columna `cantidad_original` en `posiciones_patrimonio`, tabla `ventas_patrimonio_lotes`, y redefine `generar_snapshot_patrimonio`/`registrar_venta_patrimonio` — histórico real de precio por activo, sección 15) y redesplegar la Edge Function (`npx supabase functions deploy actualizar-precios-patrimonio`, ya hace el backfill sola).
- [ ] (Opcional, recomendado) Ejecutar en el SQL Editor de Supabase para borrar filas ya muertas de `patrimonio_historico` (las de posiciones con ticker, que `generar_snapshot_patrimonio` dejó de generar en la 0017 pero las viejas se quedaron acumuladas — ver incidente del 2026-09-06 más abajo):
  ```sql
  delete from patrimonio_historico h
  using posiciones_patrimonio p
  where h.posicion_id = p.id and p.ticker is not null;
  ```
- [x] Ejecutar `supabase/migrations/0018_patrimonio_ajustar_cuenta.sql` en el SQL Editor de Supabase (RPC `ajustar_cuenta_patrimonio` — "Añadir/reducir importe" en cuentas de saldo, sección 15).
- [ ] Ejecutar `supabase/migrations/0019_patrimonio_venta_credita_cuenta.sql` en el SQL Editor de Supabase (al vender y abonar en una cuenta existente, ahora la hace crecer en vez de crear una posición nueva — mismo motivo que la 0018).
- [ ] Ejecutar `supabase/migrations/0020_patrimonio_cuenta_gastos_unica.sql` en el SQL Editor de Supabase (índice único que impide más de una Cuenta Corriente "Gastos" activa por usuario — ver incidente del 2026-09-11 más abajo).
- [ ] (Una sola vez) Consolidar en el SQL Editor las cuentas de saldo que ya tengan varias posiciones de antes de este cambio (p. ej. varias aportaciones a la misma cuenta) en una sola fila cada una — pide el diagnóstico primero, ver conversación del 2026-09-09.

## Pendiente de definir (sección 14 de REQUIREMENTS.md, no bloqueante)

- [ ] Nivel de agregación de las series temporales: el Dashboard ya no tiene gráfico de evolución (se quitó); en Visualizaciones el usuario elige el rango de meses libremente, pero sigue siendo agregación mensual fija (no semanal/anual).
- [ ] Backup/exportación de la base de datos.

## Mejoras conocidas / deuda técnica

- [ ] Los iconos de `public/icons/*.svg` son un placeholder (monograma "NB"); sustituir por un diseño real cuando se defina la identidad visual de la app.
- [ ] El "objetivo del mes" (sección 7, tarjetas "Ahorrar este mes") sigue siendo solo una referencia visual — no se aplica ni se recuerda automáticamente. Si en el futuro se quiere avisar cuando el usuario no ha destinado suficiente dinero real a un objetivo ese mes, haría falta comparar `aportaciones_objetivo` del mes en curso contra `calcularAportacionDeseada`.
- [ ] La asignación de un gasto de "Ahorro" a un objetivo solo se puede hacer sobre movimientos propios (`usuario_id` = usuario de la sesión), nunca "a nombre del otro" — por las políticas RLS de `objetivos_ahorro` (individuales, sección 7), la sesión activa no puede ver los objetivos del otro usuario para poder elegirlos.
- [ ] Tipos de `src/lib/supabase/database.types.ts` están escritos a mano porque no hay CLI de Supabase autenticado en este entorno. Cuando se disponga de `supabase login`, regenerar con:
      `npx supabase gen types typescript --project-id koyvpbsnrxqheaugkxbu --schema public > src/lib/supabase/database.types.ts`
- [ ] `npm audit` reporta un aviso "high" sobre `react-router-dom` (GHSA-qwww-vcr4-c8h2, CSRF en "RSC Mode"). No aplica a esta app: es una SPA client-side con `HashRouter`, sin React Server Components ni server actions. Revisar si sale una versión >8.2.0 que lo resuelva sin downgrade.
- [ ] Sin tests de integración de RLS todavía (ver sección "Verificación end-to-end" del plan de arquitectura: probar con las 2 cuentas reales que `privado`/`compartido` se respeta).
- [ ] La selección de "líneas" en Visualizaciones vive en un contexto de React montado en `AppShell` (`lib/visualizaciones/VisualizacionesProvider.tsx`): sobrevive a cambiar de pestaña dentro de la misma sesión, pero se pierde al cerrar/recargar la app (a propósito, no se persiste en `localStorage`). Si en el futuro se quiere que sobreviva también a cerrar la app, sería cuestión de guardar ese mismo estado en `localStorage`.
- [ ] El límite de 8 líneas en Visualizaciones es deliberado: es el techo real de la paleta categórica validada por daltonismo/contraste (ver `src/components/charts/colorsCategoricos.ts`). No se puede subir más sin repetir color entre dos líneas — si hiciera falta más, habría que rediseñar como tabla o pequeños múltiplos en vez de generar más colores.
- [ ] Tras cada despliegue, la web puede tardar unos minutos (hasta ~10) en reflejar los cambios: la CDN de GitHub Pages cachea `index.html`/`sw.js` hasta 10 minutos (`Cache-Control: max-age=600`, no configurable desde este repo), y encima el service worker de la PWA tiene que darse cuenta de que hay una versión nueva. Se mitigó parcialmente registrando el SW a mano en `main.tsx` con una comprobación de actualización cada minuto (antes solo se comprobaba en momentos puntuales), pero el límite de la CDN no se puede eliminar sin cambiar de hosting.
- [ ] El deploy a GitHub Pages puede fallar de forma puntual quedándose en "deployment_queued" hasta hacer timeout (visto el 2026-08-06, resuelto solo al reintentar `gh workflow run deploy.yml` unos minutos después). Parece un atasco temporal del backend de Pages, no relacionado con el código — si vuelve a pasar, reintentar pasados unos minutos.
- [ ] Los periodos de "mes" personalizado (sección 3, `es_primer_dia_mes`) se recalculan siempre a partir de las nóminas marcadas actuales: si se edita la fecha de una nómina marcada, se desmarca la casilla, o se borra ese movimiento, los rangos de los meses que dependían de esa marca cambian retroactivamente (a diferencia del mes de calendario fijo de antes). Es un comportamiento intencionado del diseño, no un bug, pero puede hacer que un gráfico de Visualizaciones ya visto cambie tras editar una nómina antigua.
- [ ] El ticker de Criptomoneda debe ser el ID de CoinGecko (p. ej. `bitcoin`), no el símbolo bursátil (`BTC`) — si se escribe mal, esa posición simplemente no se actualiza (sin aviso visible en la app todavía) y se queda con el último precio guardado.
- [ ] **Se cambió de Twelve Data a Yahoo Finance (2026-09-02)**: su plan gratuito no cubría casi ningún mercado europeo (ni ETFs ni acciones ni fondos, con error "available starting with the Grow or Venture plan" a pesar de aparecer en su buscador gratuito). Yahoo Finance sí cubre estos mercados (endpoint interno no oficial/no documentado, mismo que usa `yfinance` de Python) — actualizar el Ticker de las posiciones existentes al nuevo formato (símbolo de Yahoo Finance CON sufijo de mercado incluido, ya no hace falta el campo Mercado):
  - Air France KLM: `AF` → `AF.PA`
  - VanEck Uranium and Nuclear (NUKL): `NUKL` → `NUKL.DE`
  - Vanguard Global Stock Index: `0P00011HBM` → `0P00011HBM.F`
  - Vanguard Global Bond Index: `0P00012I6E` → `0P00012I6E.F`
  - Oro (Xetra-Gold): `GOLD` → `4GLD.DE`
  - Plata (db Physical Silver ETC, mismo ISIN `DE000A1EK0J7` que ya se tenía): → `XAD2.DE`
  - Bitcoin: sin cambios (`bitcoin`, sigue siendo CoinGecko)
- [ ] Yahoo Finance no tiene límite de peticiones documentado (a diferencia de Twelve Data, 8 créditos/minuto en el plan gratuito), pero al no ser una API oficial tampoco hay garantía — si empieza a fallar de forma sistemática con `error_precio` de tipo 429/999, podría ser un bloqueo por volumen de peticiones.
- [ ] El backfill del histórico de Patrimonio (`generar_snapshot_patrimonio`) para posiciones SIN ticker y SIN tae (p. ej. Cuenta Corriente, o un "por unidad" sin ticker con precio manual) rellena los días saltados usando el precio actual VIGENTE en el momento de ejecutarse, no el valor real que tuviera la posición ese día pasado — no hay ninguna fuente de precio de mercado que consultar para esos tipos. Las posiciones CON ticker ya no tienen este problema (histórico real en `precios_historico_activo`, ver sección 15); las que tienen TAE tampoco (la fórmula de interés da el valor exacto de cualquier día pasado).
- [ ] El histórico de CoinGecko (`/coins/{id}/market_chart`) tiene un límite de días hacia atrás en el plan gratuito — si una posición de Criptomoneda tiene una fecha de compra muy antigua, el backfill puede traer menos días de los pedidos en vez de todo el rango.
- [ ] La conversión histórica de USD a EUR (backfill de un ticker en USD) empareja cada día por fecha EXACTA con el histórico de `EURUSD=X`: si un día concreto no tiene tipo de cambio para esa misma fecha (poco frecuente, distintos festivos de mercado), ese punto del histórico se descarta en vez de aproximarlo con el día más cercano.
- [x] **Incidente (2026-09-06): "Ahorro Sabadell" (y "Gastos") desaparecían de "Histórico por posición" a partir de mayo, y "Histórico del patrimonio" daba saltos raros y terminaba por debajo del patrimonio total real.** Causa: Supabase/PostgREST no devuelve más de 1000 filas por consulta si no se pagina explícitamente (de más quedan descartadas en silencio, sin error) — `patrimonio_historico` acumuló miles de filas ya muertas de las posiciones CON ticker (las que generaba `generar_snapshot_patrimonio` antes de que la 0017 dejara de escribir nuevas para ellas; las viejas nunca se borraron), superó las 1000 filas, y `fetchPatrimonioHistorico()` devolvía solo las más antiguas — cortando a mitad de año justo las posiciones que SÍ siguen dependiendo de esa tabla (cuentas/TAE). Arreglado: `fetchPatrimonioHistorico`/`fetchPreciosHistoricoActivo`/`fetchVentasPatrimonioLotes` ahora paginan con `.range()` hasta traer todas las filas (`src/lib/supabase/paginacion.ts`), y `crecimientoDesdeInicioAnio` también usa ya el precio real de las posiciones con ticker en vez de `patrimonio_historico`. Queda pendiente (arriba, opcional) borrar las filas muertas ya acumuladas.
- [ ] El snapshot diario de Patrimonio no se reescribe una vez generado ese día: si editas el "Precio Actual" de una posición "por unidad" sin ticker (precio manual) después de que la app ya haya generado el snapshot de hoy, el punto de "hoy" en el histórico se queda con el valor anterior hasta el snapshot de mañana. Las cuentas "de saldo" ya no tienen este problema al usar "Añadir/reducir importe" (sección 15): ese flujo corrige `patrimonio_historico` para la fecha exacta en la misma operación.
- [x] **Incidente (2026-09-11): la cuenta "Gastos" mostraba 374,16 € en Patrimonio pero 139,83 € en Inicio.** Causa: `useSincronizarCuentaGastos` no tenía ninguna protección contra que dos pestañas/dispositivos, al ver "todavía no existe" casi a la vez, crearan cada uno su propia fila "Gastos" — Alvaro tenía dos (139,83 € del 4 de septiembre, sincronizada, y 234,33 € del 10, huérfana), y `agruparPorActivo` las sumaba en la tarjeta (374,16 €) mientras Inicio mostraba el balance en vivo real. Arreglado con un índice único (`0020_patrimonio_cuenta_gastos_unica.sql`) que impide crear una segunda, y el hook ahora sincroniza siempre la más antigua si por lo que sea hay más de una, y no falla visiblemente si el índice bloquea una creación duplicada.
- [x] **(2026-09-09) Se retiró la TAE del formulario y "aportar a una cuenta" dejó de crear una posición nueva.** Antes, cada aportación a Cuenta Corriente/Remunerada/Ahorro/Fondo Monetario creaba una posición separada (con su propia TAE/fecha) agrupada visualmente — esto hacía que reconstruir "cuánto había realmente el día X" fuera complejo, y el cálculo de TAE se reiniciaba de forma confusa al aportar. Ahora esos 4 tipos son siempre una única posición que crece/decrece con "Añadir/reducir importe"; el formulario ya no ofrece fijar una TAE nueva (una posición que aún la tuviera se cristaliza al primer ajuste o edición); y sus tarjetas ya no muestran P&L.
- [ ] Sin tests de integración para `generar_snapshot_patrimonio` (RPC de Supabase) ni para las políticas RLS de `posiciones_patrimonio`/`patrimonio_historico` — mismo hueco ya conocido para el resto de RLS del proyecto (ver nota más arriba).
- [ ] "Financiar con una cuenta" (sección 15) solo ofrece cuentas con una única aportación activa: si una cuenta ya tiene varias aportaciones, no aparece en el desplegable — retirar de una con varias es ambiguo (¿de cuál se descuenta?) y se deja fuera de esta primera versión, se ajusta a mano editando esa posición.
- [ ] No hay pantalla de historial de ventas todavía: `ventas_patrimonio` ya registra cada venta (fecha, cantidad, precio, coste base, ganancia realizada), pero solo se guarda — verlas requiere consultar la tabla directamente hasta que se construya esa pantalla.
- [ ] Sin tests de integración para `registrar_venta_patrimonio`/`crear_posicion_financiada_patrimonio` (RPC de Supabase) ni para las políticas RLS de `ventas_patrimonio` — mismo hueco de siempre para RPC/RLS de este proyecto.
